spring:
  application:
    name: frontui
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: frontui-client
            client-secret: ${FRONTUI_CLIENT_SECRET}
            authorization-grant-type: client_credentials
            scope: openid
        provider:
          keycloak:
            token-uri: http://bankapp-keycloak:8080/realms/bankapp/protocol/openid-connect/token

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    info:
      enabled: true
    prometheus:
      enabled: true
    metrics:
      enabled: true
    health:
      show-details: always
  zipkin:
    tracing:
      endpoint: http://bankapp-zipkin:9411/api/v2/spans
  tracing:
    sampling:
      probability: 1.0
  metrics:
    export:
      prometheus:
        enabled: true
    distribution:
      percentiles-histogram:
        http.server.requests: true

rsa:
  public-key: classpath:certs/public.pem

server:
  port: 8080
  servlet:
    context-path: /frontui

logging:
  level:
    root: INFO
    ru.yandex.practicum.frontui: DEBUG
    ru.yandex.practicum.frontui.configuration.security: DEBUG
    org.springframework.security.oauth2.client: DEBUG