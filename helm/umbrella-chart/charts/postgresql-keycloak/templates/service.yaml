apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgresql-keycloak.fullname" . }}
  labels:
    {{- include "postgresql-keycloak.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None  # Headless service для StatefulSet
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5432
      protocol: TCP
      name: postgresql
  selector:
    {{- include "postgresql-keycloak.selectorLabels" . | nindent 4 }}
