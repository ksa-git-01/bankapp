apiVersion: v1
kind: Secret
metadata:
  name: {{ include "generator.fullname" . }}-keycloak
  labels:
    {{- include "generator.labels" . | nindent 4 }}
type: Opaque
data:
  client-secret: {{ .Values.keycloak.client.secretValue | b64enc | quote }}