spring:
  datasource:
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
  cloud:
    consul:
      enabled: false
      discovery:
        enabled: false

jwt:
  expiration: 3600000